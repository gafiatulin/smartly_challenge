'use strict'const config = {    host            : 'x.x.x.x',    user            : 'user',    password        : 'password',    db              : 'mydb',    multiStatements : true,    compress        : true  };let app = require('koa')();app.use(require('./error-handling-middleware.js')());app.use(require('./query-validating-middleware.js')());app.use(require('./mariasql-middleware.js')(config));app.use(require('./fetch-middleware.js')());app.listen(3000);